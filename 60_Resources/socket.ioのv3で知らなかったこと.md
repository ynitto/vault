---
title: "socket.ioのv3で知らなかったこと"
source: "https://zenn.dev/dove/scraps/8112539765d869"
author:
published: 2020/12/12にクローズ
created: 2026-05-01
description: "ハトすけさんのスクラップ"
tags:
  - "clippings"
---
### 要約
本記事はSocket.io v3の主要機能と開発上の注意点をまとめた技術メモです。サーバー・クライアントの初期化から、ルーム、ミドルウェア、CORS、エラーハンドリング、バイナリ転送など、実践的な実装パターンを解説しています。

### 要点まとめ

#### サーバー・クライアント共通
- **初期化**: サーバーはHTTP/Express等と共存可能。クライアントはドメインに応じて`io()`で接続。
- **通信**: `socket.emit`のほか、`socket.send`による`message`イベント利用も可能。
- **バイナリ対応**: JSONシリアライズ不要で直接オブジェクト送信可能（Map/Setは要シリアライズ）。

#### サーバーサイド機能
- **ルーム**: `socket.join('name')`で参加。`socket.rooms`で管理。
- **ミドルウェア**: `io.use()`で実装。`next()`で進行、`next(Error)`で接続拒否。
- **プロパティ追加**: `socket`インスタンスに直接ユーザー情報等を保持可能。
- **認証**: `auth`オブジェクトでトークン等を渡せる。

#### 運用とベストプラクティス
- **複数サーバー**: ロードバランサーの「スティッキーセッション」とRedisアダプターが必須。
- **CORS**: v3から設定が必須化。
- **エラー対応**: `connect_error`時のリトライは手動で実装が必要。
- **揮発性イベント**: `socket.volatile.emit`で再接続時のバッファ蓄積を防ぐ（位置情報等に有効）。
- **アンチパターン**: `connect`イベント内にイベントリスナーを定義すると、リコネクションのたびにリスナーが重複登録されるため避けること。
